function fibonacciSeries(x){

    const sequence = [0,1];
    let newArray =[];
    let count =0;
    newArray= newArray.concat(sequence);
    for(let i=0;i<x.length;i++){
        count=;
        newArray.push(count);
    }
}

const result = fibonacciSeries(5)